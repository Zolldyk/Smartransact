// Evidence — nav stub. The committed-run table (stat cards + one row per bundle,
// explorer links, drill-down) lands in Story 8.4.
export default function EvidencePage() {
  return (
    <div className="wrap-narrow stub">
      <div className="eyebrow">Evidence</div>
      <h1>The committed run</h1>
      <p>The verifiable evidence table — summary stats and every bundle, each slot linked to a Solana explorer — arrives in Story 8.4.</p>
    </div>
  );
}

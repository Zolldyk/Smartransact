// Architecture — nav stub. The hosted architecture document (FR31, diagrams
// embedded) is delivered as a page in Story 8.5.
export default function ArchitecturePage() {
  return (
    <div className="wrap-narrow stub">
      <div className="eyebrow">Architecture</div>
      <h1>How it works</h1>
      <p>The hosted architecture document — topology, the evidence pipeline, and the autonomous retry agent — arrives as a page in Story 8.5.</p>
    </div>
  );
}

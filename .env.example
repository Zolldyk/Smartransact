# Google AI Studio (Gemini) free-tier API key — used by the agent's LLM provider.
# Get one at https://aistudio.google.com/apikey . Never commit the real .env.
GEMINI_API_KEY=your-gemini-api-key-here

# Absolute path to the local Solana keypair JSON file (gitignored), funded via testnet faucet.
# Generated by scripts/setup-keypair.sh (added in a later story).
KEYPAIR_PATH=/absolute/path/to/keypair.json

# SolInfra gRPC endpoint — only required when using the solinfra-grpc profile.
# Swap active profile in smartransact.config.json to "solinfra-grpc" and set this.
SOLINFRA_GRPC_ENDPOINT=your-solinfra-grpc-endpoint-here
